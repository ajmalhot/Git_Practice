apiVersion: v1
kind: Pod
metadata:
  name: myapp
  labels:
    app: myapp-nginx
    tier: frontend
spec:
  containers:
    - name: nginx
      image: nginx:latest
    - ports:
        port: 80
        targetport: 30004
This is k8s practice file in branch1
